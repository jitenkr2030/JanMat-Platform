{
  "name": "janmat-fullstack",
  "version": "1.0.0",
  "description": "JanMat - Public Opinion & Citizen Voice App for India",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd mobile-app && npm start",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd backend && npm run build",
    "build:client": "cd mobile-app && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../mobile-app && npm install && cd ../admin-dashboard && npm install"
  },
  "keywords": [
    "democracy",
    "polls",
    "india",
    "citizen-voice",
    "public-opinion"
  ],
  "author": "MiniMax Agent",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}